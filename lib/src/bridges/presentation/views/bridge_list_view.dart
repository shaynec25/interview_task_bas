import 'package:flutter/material.dart';

class BridgeListView extends StatelessWidget {
  const BridgeListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
